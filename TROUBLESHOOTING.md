# 🐛 故障排除指南

## Request ID 错误排查

如果你遇到了 Request ID 相关的错误，请提供以下信息：
- 错误发生的平台（Netlify / GitHub / Supabase / 其他）
- 完整的错误信息
- 错误发生的时间点（部署时 / 运行时 / 其他）

---

## 🔍 常见错误及解决方案

### 1. Netlify 部署错误

#### 错误：Build failed

**可能原因：**
- 依赖安装失败
- 构建命令错误
- 环境变量未配置

**解决方法：**
1. 检查 Netlify 部署日志
2. 确认 Install command 设置为：`npm install --legacy-peer-deps`
3. 确认 Build command 为：`npm run build`
4. 检查环境变量是否配置

#### 错误：Environment variable not found

**解决方法：**
1. Netlify → Site settings → Environment variables
2. 添加 `VITE_SUPABASE_URL` 和 `VITE_SUPABASE_ANON_KEY`
3. 重新部署

---

### 2. GitHub 推送错误

#### 错误：Authentication failed

**解决方法：**
- 使用 Personal Access Token 而不是密码
- 检查 Token 权限是否包含 `repo`

#### 错误：Remote origin already exists

**解决方法：**
```bash
git remote remove origin
git remote add origin https://github.com/your-username/repo.git
```

---

### 3. Supabase 连接错误

#### 错误：Supabase 配置缺失

**解决方法：**
1. 检查环境变量是否正确配置
2. 确认 Supabase 项目状态为 Active
3. 检查 API 密钥是否正确

#### 错误：Database connection failed

**解决方法：**
1. 确认数据库表已创建（执行了 SQL 迁移脚本）
2. 检查 Supabase 项目状态
3. 查看 Supabase 日志

---

### 4. 构建错误

#### 错误：Module not found

**解决方法：**
```bash
npm install --legacy-peer-deps
npm run build
```

#### 错误：TypeScript errors

**解决方法：**
1. 检查 `tsconfig.json` 配置
2. 确认所有依赖已安装
3. 查看具体错误信息

---

## 📋 检查清单

### 部署前检查：
- [ ] Git 用户信息已配置
- [ ] 代码已推送到 GitHub
- [ ] Supabase 项目已创建
- [ ] 数据库表已创建
- [ ] 环境变量已配置
- [ ] 项目本地构建成功

### 部署时检查：
- [ ] Netlify 授权已连接 GitHub
- [ ] 仓库在 Netlify 中可见
- [ ] 构建命令正确
- [ ] 环境变量已添加
- [ ] 部署日志无错误

### 部署后检查：
- [ ] 网站可以访问
- [ ] 浏览器控制台无错误
- [ ] Supabase 连接正常
- [ ] 功能测试通过

---

## 🆘 获取帮助

如果问题仍未解决，请提供：

1. **错误平台**：Netlify / GitHub / Supabase / 其他
2. **完整错误信息**：复制完整的错误消息
3. **Request ID**：如果提供了 Request ID
4. **操作步骤**：你执行了什么操作
5. **截图**：如果有错误截图

---

## 📚 相关文档

- **Netlify 部署**: `NETLIFY_START.md`
- **GitHub 上传**: `GITHUB_SETUP_COMPLETE.md`
- **Supabase 设置**: `SUPABASE_SETUP.md`


# 🖼️ Netlify 设置 Install command - 可视化指南

## 📋 在 Netlify 导入项目时的设置页面

当你导入 GitHub 仓库后，Netlify 会显示一个配置页面，类似这样：

```
┌─────────────────────────────────────────┐
│  Configure build settings               │
├─────────────────────────────────────────┤
│                                         │
│  Branch to deploy:                      │
│  [main ▼]                              │
│                                         │
│  Build command:                         │
│  [npm run build]                        │
│                                         │
│  Publish directory:                     │
│  [dist]                                 │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │  Show advanced                  │   │ ← 点击这里！
│  └─────────────────────────────────┘   │
│                                         │
└─────────────────────────────────────────┘
```

---

## 🔽 点击 "Show advanced" 后

页面会展开，显示更多选项：

```
┌─────────────────────────────────────────┐
│  Configure build settings               │
├─────────────────────────────────────────┤
│                                         │
│  Branch to deploy:                      │
│  [main ▼]                              │
│                                         │
│  Build command:                         │
│  [npm run build]                        │
│                                         │
│  Publish directory:                     │
│  [dist]                                 │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │  Hide advanced                  │   │ ← 现在显示 "Hide"
│  └─────────────────────────────────┘   │
│                                         │
│  Install command:                       │ ← 找到了！
│  [________________]                     │ ← 在这里输入
│                                         │
│  输入: npm install --legacy-peer-deps   │
│                                         │
└─────────────────────────────────────────┘
```

---

## ✅ 正确填写

在 **"Install command"** 输入框中输入：

```
npm install --legacy-peer-deps
```

---

## 🔄 如果已经部署了，如何修改？

### 方法：在站点设置中修改

1. **进入站点**
   - 在 Netlify 控制台，点击你的站点

2. **进入设置**
   - 点击 **"Site settings"**（站点设置）
   - 或点击右上角的设置图标

3. **找到构建设置**
   - 左侧菜单 → **"Build & deploy"**
   - 点击 **"Build settings"**

4. **编辑设置**
   - 找到 **"Build settings"** 部分
   - 点击 **"Edit settings"** 按钮

5. **修改 Install command**
   - 找到 **"Install command"** 输入框
   - 输入：`npm install --legacy-peer-deps`
   - 点击 **"Save"**

6. **重新部署**
   - 点击 **"Trigger deploy"** → **"Deploy site"**
   - 或推送新的代码到 GitHub（会自动触发部署）

---

## 💡 提示

- 如果找不到 "Show advanced"，可能 Netlify 界面已更新
- 可以直接部署，如果遇到依赖错误，再在站点设置中修改
- Install command 不是必须的，但设置后可以避免依赖冲突问题

---

## 🆘 需要帮助？

告诉我：
- 你现在在 Netlify 的哪个页面？
- 看到了什么选项？
- 我可以提供更具体的指导！


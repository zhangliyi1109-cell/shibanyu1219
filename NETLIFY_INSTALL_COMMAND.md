# 📍 Netlify 中设置 Install command 的位置

## 🎯 在哪里设置 Install command

### 步骤 1：进入部署设置页面

在 Netlify 导入项目后，你会看到一个配置页面，标题类似：
**"Configure build settings"** 或 **"Deploy settings"**

### 步骤 2：找到 "Show advanced" 或 "Advanced" 按钮

在配置页面上，找到：
- **"Show advanced"** 按钮（通常在页面底部）
- 或 **"Advanced"** 标签/链接
- 或 **"Advanced build settings"** 选项

**位置：**
- 通常在 "Build command" 和 "Publish directory" 设置的下方
- 或者在页面右侧/底部

### 步骤 3：展开高级设置

点击 **"Show advanced"** 后，会显示更多选项，包括：
- **Install command** ← 这就是你要找的！
- 其他高级选项

### 步骤 4：设置 Install command

在 **"Install command"** 输入框中，输入：
```
npm install --legacy-peer-deps
```

---

## 📸 详细步骤（图文说明）

### 在 Netlify 导入页面：

1. **基本设置区域**（默认显示）：
   ```
   Branch to deploy: [main ▼]
   Build command: [npm run build]
   Publish directory: [dist]
   ```

2. **点击 "Show advanced"** 或 **"Advanced"**：
   ```
   [Show advanced ▼]  ← 点击这里
   ```

3. **展开后显示**：
   ```
   Install command: [________________]  ← 在这里输入
   ```

4. **输入内容**：
   ```
   Install command: [npm install --legacy-peer-deps]
   ```

---

## 🔍 如果找不到 "Show advanced"

### 方法 1：滚动页面

"Show advanced" 可能在页面下方，向下滚动查找。

### 方法 2：查看所有输入框

查找任何包含 "install" 或 "command" 的输入框。

### 方法 3：部署后修改

如果已经部署了，可以：
1. 进入站点设置
2. Site settings → Build & deploy → Build settings
3. 点击 "Edit settings"
4. 找到 "Install command" 并修改

---

## 📝 完整配置示例

在 Netlify 配置页面应该设置：

```
Branch to deploy: main
Build command: npm run build
Publish directory: dist
Install command: npm install --legacy-peer-deps  ← 这里！
```

---

## ⚠️ 重要提示

- **Install command** 不是必须的，但如果遇到依赖冲突错误，必须设置
- 如果不设置，Netlify 默认使用 `npm install`
- 设置后，每次部署都会使用这个命令安装依赖

---

## 🆘 如果还是找不到

告诉我：
1. 你现在在 Netlify 的哪个页面？
2. 看到了哪些设置选项？
3. 我可以提供更具体的指导！

或者，你可以先部署，如果遇到错误，再在站点设置中修改 Install command。

